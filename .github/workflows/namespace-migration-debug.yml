name: retired namespace migration debug

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Root cause captured; superseded by owner-native-namespace-migration-v4.yml'
